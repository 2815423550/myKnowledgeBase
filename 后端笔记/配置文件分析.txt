spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/seckill?characterEncoding=utf-8&useSSL=false
spring.datasource.username=root
spring.datasource.password=1431891568

#关闭thymeleaf缓存
spring.thymeleaf.cache=false

#配置连接池
spring.datasource.hikari.pool-name=DateHikariCP
#最小空闲连接数
spring.datasource.hikari.minimum-idle=5
#空闲连接存活最大时间，默认600000（10分钟）
spring.datasource.hikari.idle-timeout=1800000
#最大存活时间，0表示永久存活，默认1800000（30分钟）
spring.datasource.hikari.max-lifetime=1800000
#最大连接数，默认10
spring.datasource.hikari.maximum-pool-size=10
#连接超时时间，默认30000（30秒）
spring.datasource.hikari.connection-timeout=30000
#测试连接是否可用的查询语句
spring.datasource.hikari.connection-test-query=SELECT 1


#配置mybatis-plus
mybatis-plus.mapper-locations=classpath*:/mapper/*Mapper.xml
mybatis-plus.type-aliases-package=com.hht.seckill.pojo

#mybatis sql打印（这里要写方法接口所在的包，不是Mapper.xml所在的包
logging.level.com.hht.seckill.mapper:debug